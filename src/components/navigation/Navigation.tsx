import { FC } from "react";
import { Box, Typography } from "@mui/material";

const Navigation: FC = () => {
  return (
    <Box component="nav">
      <Typography>Ukraine Logistics</Typography>
    </Box>
  );
};

export default Navigation;
